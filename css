/* Reset styles for all elements */
*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Comic Sans MS', cursive;
}
/* Heading styles */
h1{
    font-size: 50px;
    line-height: 64px;
    color: #222;
}
/* Styles for heading 2 */
h2{
    font-size: 46px;
    line-height: 54px;
    color: #222;
}
/* Styles for heading 4 */
h4{
    font-size: 20px;
    color: #222;
}
/* Styles for heading 6 */
h6{
    font-weight: 700;
    font-size: 12px;
}
/* Paragraph styles */
p{
    font-size: 16px;
    color: #222;
    margin: 15px 0 20px 0;
}
/* Section styles */
.section-p1 {
    padding:40px 80px;
}
.section-m1{
    margin: 40px 0;
}
/* Button styles */
button.normal{
    font-size: 14px;
    font-weight: 600;
    padding: 10px 15px;
    border-radius: 4px;
    color: #fff;
    background-color:#088178;
    cursor: pointer;
    border: none;
    outline: none;
    transition: 0.2;
}
body{
    width: 100%;
}

/* Header styles */

#header{
 display: flex;
 align-items: center;
 justify-content: space-between;
 padding: 20px 80px;
 background: rgb(131, 81, 188);
 box-shadow: 0 5px 15px rgba(0,0,0,0.06);
 z-index: 999;
 position: sticky;
 top: 0;
 left: 0;
}
/* Navbar styles */
#navbar{
    display: flex;
    align-items: center;
    justify-content: space-between;
}
/* Styles for list items in the navigation bar */
#navbar li{
    list-style: none;
    padding: 0 20px;
    position: relative;
    font-size: larger;
}
/* Styles for anchor links in the navigation bar */
#navbar li a{
    text-decoration: none;
    color:white;
    font-size: 16px;
    font-weight: 600;
    transition: 0.3s ease;
}
/* Styles for hover and active states of anchor links in the navigation bar */
#navbar li a:hover,
#navbar li a.active{
    color: rgb(47, 5, 5);
}
/* Styles for the underline effect on hover and active states of anchor links */
#navbar li a.active::after,
#navbar li a:hover::after{
content: "";
    width: 30%;
    height: 2px;
    background:  rgb(47, 5, 5);
    position: absolute;
    bottom: -4px;
    left: 20px;
}

/* Styling Banner */
#banner{
    background-image: url(img/best-home-printer-hero.jpg);
    height: 90vh;
    width: 100%;
    background-size: cover;
    background-position: top 25% right 0;
    padding: 0 80px;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
}
/* Styles for the heading 4 in the banner */
#banner h4 {
    padding-bottom: 15px;
}
/* Styles for the button in the banner */
#banner button {
    background-image: url(img/paint.webp);
    background-color: transparent;
    color: rgb(10, 17, 22);
    border: 0;
    padding: 14px 80px 14px 65px;
    background-repeat: no-repeat;
    cursor: pointer;
    font-weight: 700;
    background-size: cover;
    background-position: center;
}
/* Features styles */
#features{
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap; 

}
/* Styles for feature containers */
#features .container{
    width: 180px;
    text-align: center;
    padding: 25px 15px;
    box-shadow: 5px 5px 10px rgb(94, 89, 89);
    border: 1px solid #cce7d0;
    border-radius: 4px;
    margin: 15px 0;

}
/* Hover styles for feature containers */
#features .container:hover{
    box-shadow: 1px 1px 30px rgb(131, 129, 129);

}
/* Styles for images inside feature containers */
#features .container img{
    width: 100%;
    margin-bottom: 10px;
     
}
/* Styles for heading 6 inside feature containers */
#features .container h6{
    display: inline-block;
    padding: 9px 8px 6px 8px;
    line-height: 1;
    border-radius:  4px;
    color: rgb(17, 18, 19);
    background-color: rgb(131, 183, 226);
}
/* Different background colors for specific feature containers */
#features .container:nth-child(2) h6{
    background-color:#cdebbc;

}
#features .container:nth-child(3) h6{
    background-color:#d1e8f2;
}
#features .container:nth-child(4) h6{
    background-color:#cdd4f8;
}

#features .container:nth-of-type(5) h6 {
    background-color: #f6dbf6;
}

#features .container:nth-of-type(6) h6 {
    background-color: #fff2e5;
}
/* Hot Sales styles */
#hot-sales{
    text-align: center;
}
/* Styles for the container of hot sales products */
#hot-sales .pro-container{
    display: flex;
    justify-content: space-between;
    padding-top: 20px;
    flex-wrap: wrap;

}
/* Styles for individual hot sales products */
#hot-sales .pro{
    width: 23%;
    min-width: 250px;
    padding: 10px 12px;
    border: 1px solid #ccc;
    border-radius: 25px;
    cursor: pointer;
    box-shadow: 20px 20px 30px rgb(94, 89, 89);
    margin: 15px 0;
    transition: 0.2s ease;
    position: relative;
}
/* Hover styles for individual hot sales products */
#hot-sales .pro:hover{
    box-shadow: 5px 5px 60px  rgb(131, 129, 129);
}
/* Styles for product images in hot sales */
#hot-sales .pro img {
    width: 200px;
    height: 200px;
    border-radius: 30px;
    background-size: cover;
}
/* Styles for cart image in hot sales */
#hot-sales .pro a img {
    width: 30px; /* specify the width you want for the img/cart image */
    height: 30px; /* specify the height you want for the img/cart image */
}
/* Styles for product description in hot sales */
#hot-sales .pro .des h4{
    padding-top: 7px;
    font-size: 15px;
    font-weight: 700;
    color: #088178;
}
/* Styles for the cart container in hot sales */
#hot-sales .pro .cart {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 40px;
    height: 40px;
    line-height: 40px;
    border-radius: 50px;
    background-color: #e8f6ea;
    font-weight: 500;
    color: #088178;
}
/* Styles for the cart image in hot sales */
#hot-sales .pro .cart img {
    width: 30px;
    height: 30px;
}
/* Flag styles */
#flag {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;/* Align content to the bottom */
    align-items: flex-start;/* Align content to the left */
    text-align: left;/* Align text to the left */
    background-image: url(img/pflag.jpg);
    width: 100%;
    height: 50vh;
    background-size: cover;
    background-position: center;
    padding: 20px;
}
/* Styles for heading 4 in the flag section */
#flag h4{
    color: rgb(234, 203, 6);
}

/* Styles for heading 2 in the flag section */
#flag h2 {
    margin: 0;
    font-size: 1.5em; /* Adjust the font size as needed */
    padding-bottom: 10px; /* Add padding to the bottom of h2 */
}
/* Styles for spans within heading 2 in the flag section */
#flag h2 span {
    color: #ef3636;
}
/* Hover styles for the button in the flag section */
#flag button:hover{
    background: rgb(92, 3, 181) ;
}
/* News Update styles */
#news-update{
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    align-items: center;
    background-image: url(img/newsbg.webp);
    background-repeat: no-repeat;
    background-size: cover;
    background-color: #000;
}
/* Styles for heading 4 in the news update section */
#news-update h4 {
    font-size: 22px;
    font-weight: 700;
    color: #121010;
}
/* Styles for paragraphs in the news update section */
#news-update p {
    font-size: 16px;
    font-weight: 600;
    color: #0d1116;
}
/* Styles for spans within paragraphs in the news update section */
#news-update p span{
    color: #ffbd27;
}
/* Styles for the form container in the news update section */
#news-update .form{
    display: flex;
    width: 40%;
}
/* Styles for input fields in the news update section */
#news-update input{
    height: 3.125rem;
    padding: 0 1.25em;
    font-size: 14px;
    width: 100%;
    border: 1px solid transparent;
    border-radius: 4px;
    outline: none;
    margin-right: 10px;
}
/* Styles for the button in the news update section */
#news-update button{
    white-space: nowrap;
    background-color: #088178;
}
/* Footer styles */
footer{
	display: flex;
	flex-wrap: wrap;
	justify-content: space-between;
}
/* Styles for columns in the footer */
footer .col {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	margin-bottom: 20px;
}
/* Styles for the logo in the footer */
footer .logo{
	margin-bottom: 30px;
}
/* Styles for heading 4 in the footer */
footer h4{
	font-size: 14px;
	padding-bottom: 20px;
}
/* Styles for paragraphs in the footer */
footer p{
	font-size: 13px;
	margin: 0 0 8px 0;
}
/* Styles for links in the footer */
footer a{
	font-size: 13px;
	text-decoration: none;
	color: #222;
	margin-bottom: 10px;
}
/* Styles for the follow section in the footer */
footer.follow{
	margin-top: 20px;
}
/* Styles for icons in the footer */
footer .icon{
	color: #465b52;
	padding-right: 4px;
	cursor: pointer;
}
/* Styles for installation images in the footer */
footer .install .row img{
	border: 1px solid #088178;
	border-radius: 6px;
}
/* Styles for individual installation images in the footer */
footer .install img{
	margin: 10px 0 15px 0;
}
/* Styles for hover effect on follow icons in the footer */
footer .follow .icon img:hover{
	color: #088178;
}
/* Styles for hover effect on links in the footer */
footer a:hover{
	color: #088178;
}
/* Styles for the copyright section in the footer */
footer .copyright{
	width: 100%;
	text-align: center;
}

/* Shop Banner styles */
#shop-banner{
    background-image: url(img/pflag.jpg);
    height: 55vh;
    width: 100%;
    background-size: cover;
    padding: 20px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    align-items: flex-start;
    text-align: left;
}
/* Page Number styles */
#page-no {
    text-align: center;
}
/* Styles for pagination links */
#page-no a {
    text-decoration: none;
    background-color: #088178;
    padding: 15px 20px;
    color: #fff;
    font-weight: 600;
    border-radius: 4px;
}

/* Single Product Description styles */

#prodesc{
    display: flex;
    margin-top: 20px;
}
/* Styles for the single product image container */
#prodesc .single-pro-img{
    width: 40%;
    margin-right: 50px;

}
/* Styles for a group of small images */
.small-img-group{
    display: flex;
    justify-content: space-between;
}
/* Styles for individual small images */
.small-img-col{
    flex-basis: 24%;
    cursor: pointer;
    border: 1px solid #827e7e;
    border-radius: 4px;
}
/* Styles for the single product description section */
#prodesc .single-pro-desc{
    width: 50%;
    padding-top: 30px;
}
/* Styles for the heading in the single product description */
#prodesc .single-pro-desc h4{
    padding: 40px 0 20px 0;
}
/* Styles for the select element in the single product description */
#prodesc .single-pro-desc h2{
font-size: 26px;
color: #088178;
}
#prodesc .single-pro-desc select{
    display: block;
    padding: 5px 10px;
    margin-bottom: 10px;
}
/* Styles for the input element in the single product description */
#prodesc .single-pro-desc input{
    width: 50px;
    height: 47px;
    padding-left: 10px;
    font-size: 16px;
    margin-right: 10px;
}
/* Styles for spans within the single product description */
#prodesc.single-pro-desc span{
    line-height: 25px;
}







